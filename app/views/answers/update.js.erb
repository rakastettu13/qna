$('.answer-<%= "#{answer.id}" %> > .answer-update-errors').html('<%= render 'shared/errors', resource: answer %>');

<% unless answer.errors.present? %>
  $('.answer-<%= "#{answer.id}" %>').replaceWith('<%= j render answer %>');
<% end %>
